name: PR Validation

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - reopened

jobs:
  pr-validation:
    if: github.actor != 'dependabot[bot]'
    uses: anket-cd/shared-workflows/.github/workflows/pr-checks.yml@master # pinned to latest
    secrets: inherit
    with:
      subjectPattern: '^.+$'
